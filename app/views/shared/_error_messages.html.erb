<% if @stovemaint.errors.any? %>
  <div id="error_explanation">
    <div class="alert alert-danger">
      The form contains <%= pluralize(@stovemaint.errors.count, "error") %>.
    </div>
    <ul>
    <% @stovemaint.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>
